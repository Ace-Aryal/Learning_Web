// append -> appends strings and elements , can append multiple elements and strings
// appendChild -* appends only one element at a time and not other things

// textContent-  contains every spaces , indentation, and hidden texts too 
// innerText - only the text, that's displayed on screen
// innerHTML - onlh way to render page with html code using js, can cause security problems from usera
// better create element , use innerText and append it

// element.getAttribute("id") or element.id 
// classList.toggle( "class", boolean) useful for toogling , can toggle only one class at a time 
//Yes, both classList.add() and classList.remove() 
//can handle multiple classes at once. You can pass 
//multiple class names as arguments, and the methods //will add or remove all of them simultaneously