import React from 'react'

const DefaultDashboard = () => {
    return (
        <div>Default Dashboard | fallback for dashboard/page as it is technically a slot and asks for a matched route </div>
    )
}

export default DefaultDashboard