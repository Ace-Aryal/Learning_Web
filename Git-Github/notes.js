// Git is a VCS software whereas Github is a online hosting service for git repositories,
//  its not the only one VCS software and github is also not single service provider
git config --global code.editor "code --wait"

// use gitignore "to ignore track of files" // not pushed to github
//  and gitkeep files "to ignore empty folder"